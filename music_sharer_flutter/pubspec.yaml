name: music_sharer_flutter
description: "Flutter app to listen to WebRTC audio broadcasts from VibeP2P"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_webrtc: 0.11.7  # Downgraded for Android SDK compatibility
  web_socket_channel: ^3.0.1
  provider: ^6.1.2
  shared_preferences: ^2.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
